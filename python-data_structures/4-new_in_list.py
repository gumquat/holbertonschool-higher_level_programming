#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    if my_list:
        buffer = []
        for num in buffer:
            buffer.append(num)
        if not idx < 0 and not idx >= len(my_list):
            buffer[idx] = element
            return buffer
