#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    if my_list:
        _buffer = []
        for num in _buffer:
            _buffer.append(num)
        if not idx < 0 and not idx >= len(my_list):
            _buffer[idx] = element
            return _buffer
